class Article {
  constructor(id, title, date, content, image) {
    (this.id = id),
      (this.title = title),
      (this.date = date),
      (this.content = content),
      (this.image = image);
  }
}

export default Article;
